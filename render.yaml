# Render Blueprint — deploy backend from repo root with rootDir: backend
# See: https://render.com/docs/blueprint-spec
services:
  - type: web
    name: multi-player-tag-backend
    runtime: node
    plan: free
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
